import { DEFAULT_ASSET, DEFAULT_CHAIN_ID } from './default';

export const defaultSendState = {
  asset: DEFAULT_ASSET,
  amount: 0,
  chainID: DEFAULT_CHAIN_ID,
};
export const defaultReceiveState = {
  asset: DEFAULT_ASSET,
  amount: 0,
  chainID: DEFAULT_CHAIN_ID,
};
export const defaultFeeState = {
  asset: DEFAULT_ASSET,
  amount: 0,
  chainID: DEFAULT_CHAIN_ID,
};
